meta {
  name: Login
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/login
  body: json
  auth: none
}

body:json {
  {
    "username": "admin",
    "password": "password"
  }
  
}

script:post-response {
  bru.setEnvVar("sessionToken", res.getBody());
}
