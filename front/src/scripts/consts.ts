
export enum NavBarSection {
    ACCOUNT,
    GAMES,
    SCORES
}

export enum Injects {
    USERNAME = "username"
}

export enum GameLobbySelection {
    CREATE_GAME,
    JOIN_PUBLIC,
    JOIN_PRIVATE
}
